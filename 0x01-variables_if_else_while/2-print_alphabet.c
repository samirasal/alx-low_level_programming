#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase, followed by a new line
 * Return: Always Success
 *
 */

int main(void)
{
	char c;

	while (c = 'a' && c <= 'z', c++)
	{
	putchar(c);
	}
	return (0);
}
