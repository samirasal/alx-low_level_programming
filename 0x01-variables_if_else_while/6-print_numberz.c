#include <stdio.h>
#include <stdlib.h>

/**
 * main - Prints all single digits starting from 0
 * Return: Always Success
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
	putchar(num);
	}
	putchar('\n');
	return (0);
}
