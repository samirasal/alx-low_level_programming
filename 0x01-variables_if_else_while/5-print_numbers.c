#include <stdio.h>

/**
 * main - Prints single numbers starting from 0
 * Return: Always Success
 *
 */

int main(void)
{
	int num = 0;

	for (num = 0; num < 10; num++)
	{
	printf("%d", num);
	}
	printf("\n");
	return (0);
}

